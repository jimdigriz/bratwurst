DOMAIN=localnet

# {pppoa,pppoe,dhcp}
NETWORK=pppoe

PPP_USER=test123
PPP_PASS=test456

# 'eth' (cable) or 'atm' (xDSL)
UPLINK_TYPE=atm
# 0..9, eg. 0 -> eth0/atm0
UPLINK_PORT=0

# pppoa:0,32 pppoe:8,35
ATM_VPI=8
ATM_VCI=35
# {llc,vcmux}
ATM_ENCAP=vcmux

# if DHCPv6-PD is unavailable, provide your prefix manually here
# 0->pppoa, 1->pppoe
#PREFIX=fd00:cafe:0::/48
