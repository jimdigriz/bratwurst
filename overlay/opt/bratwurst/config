# {pppoa,pppoe,dhcp}
NETWORK=pppoa

PPP_USER=test123
PPP_PASS=test456

# {eth,atm,wan0}
UPLINK_TYPE=atm
# 0..9, eg. 0 -> eth0, atm0, wan0, ...
UPLINK_PORT=0

ATM_VPI=8
ATM_VCI=35
# {llc,vcmux}
ATM_ENCAP=vcmux
