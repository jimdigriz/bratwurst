diff -u -r a/configure.ac b/configure.ac
--- a/configure.ac
+++ b/configure.ac
@@ -19,6 +19,11 @@
 	      [CONFIG_DEBUG="y"])
 AC_SUBST([CONFIG_DEBUG])
 
+AC_ARG_ENABLE([cli],
+	      AS_HELP_STRING([--disable-cli], [Disable interactive CLI]),
+              [CONFIG_CLI="n"], [CONFIG_CLI="y"])
+AC_SUBST([CONFIG_CLI])
+
 # Checks for programs.
 AC_PROG_CC
 AC_PROG_MKDIR_P
@@ -71,8 +76,10 @@
 AC_CHECK_LIB([gmp], [__gmpz_init], ,
 	     AC_MSG_ERROR([No suitable version of libgmp found]))
 
-AC_CHECK_LIB([readline], [readline], ,
-	     AC_MSG_ERROR([No suitable version of libreadline found]))
+AS_IF([test "x$enable_cli" != "xno"], [
+	AC_CHECK_LIB([readline], [readline], ,
+	             AC_MSG_ERROR([No suitable version of libreadline found]))
+])
 
 # Checks for header files.
 AC_HEADER_STDC
diff -u -r a/include/nftables.h b/include/nftables.h
--- a/include/nftables.h
+++ b/include/nftables.h
@@ -31,9 +31,11 @@
 extern const char *include_paths[INCLUDE_PATHS_MAX];
 
 struct parser_state;
+#ifdef HAVE_LIBREADLINE
 extern int cli_init(struct parser_state *state);
 extern void cli_exit(void);
 extern void cli_display(const char *fmt, va_list ap) __fmtstring(1, 0);
+#endif
 
 enum nftables_exit_codes {
 	NFT_EXIT_SUCCESS	= 0,
diff -u -r a/Makefile.defs.in b/Makefile.defs.in
--- a/Makefile.defs.in
+++ b/Makefile.defs.in
@@ -40,3 +40,5 @@
 ifeq ($(DEBUG),y)
 CFLAGS		+= -g -DDEBUG
 endif
+
+CLI		= @CONFIG_CLI@
diff -u -r a/src/main.c b/src/main.c
--- a/src/main.c
+++ b/src/main.c
@@ -48,7 +48,11 @@
 	OPT_INVALID		= '?',
 };
 
+#ifdef HAVE_LIBREADLINE
 #define OPTSTRING	"hvf:iI:vna"
+#else
+#define OPTSTRING	"hvf:I:vna"
+#endif
 
 static const struct option options[] = {
 	{
@@ -64,10 +68,12 @@
 		.val		= OPT_FILE,
 		.has_arg	= 1,
 	},
+#ifdef HAVE_LIBREADLINE
 	{
 		.name		= "interactive",
 		.val		= OPT_INTERACTIVE,
 	},
+#endif
 	{
 		.name		= "numeric",
 		.val		= OPT_NUMERIC,
@@ -103,7 +109,9 @@
 "  -v/--version			Show version information\n"
 "\n"
 "  -f/--file <filename>		Read input from <filename>\n"
+#ifdef HAVE_LIBREADLINE
 "  -i/--interactive		Read input from interactive CLI\n"
+#endif
 "\n"
 "  -n/--numeric			When specified once, show network addresses numerically.\n"
 "  				When specified twice, also show Internet services,\n"
@@ -323,9 +331,11 @@
 		scanner = scanner_init(&state);
 		if (scanner_read_file(scanner, filename, &internal_location) < 0)
 			goto out;
+#ifdef HAVE_LIBREADLINE
 	} else if (interactive) {
 		cli_init(&state);
 		return 0;
+#endif
 	} else {
 		fprintf(stderr, "%s: no command specified\n", argv[0]);
 		exit(NFT_EXIT_FAILURE);
diff -u -r a/src/Makefile.in b/src/Makefile.in
--- a/src/Makefile.in
+++ b/src/Makefile.in
@@ -3,7 +3,9 @@
 nft-destdir		:= @sbindir@
 
 nft-obj			+= main.o
+ifeq ($(CLI),y)
 nft-obj			+= cli.o
+endif
 nft-obj			+= rule.o
 nft-obj			+= statement.o
 nft-obj			+= datatype.o
